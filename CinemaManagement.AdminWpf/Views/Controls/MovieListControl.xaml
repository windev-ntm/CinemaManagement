<UserControl x:Class="CinemaManagement.AdminWpf.Views.Controls.MovieListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CinemaManagement.AdminWpf.Views.Controls"
             xmlns:controls="clr-namespace:CinemaManagement.AdminWpf.ViewModels.Controls"
             xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
             d:DataContext="{d:DesignInstance Type=controls:MovieListViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <StackPanel>
        <behaviors:Interaction.Triggers>
            <behaviors:EventTrigger EventName="Loaded">
                <behaviors:InvokeCommandAction Command="{Binding LoadCommand}" />
            </behaviors:EventTrigger>
        </behaviors:Interaction.Triggers>
        
        <DataGrid ItemsSource="{Binding Movies}" />
        <TextBlock Text="{Binding Test}" />
    </StackPanel>
</UserControl>
